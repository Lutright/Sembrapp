import 'package:flutter/material.dart';

/// Colores compartidos del módulo de alfabetización (coherentes con la guía por voz).
abstract final class AlfabetizacionUiColors {
  /// Botón principal: continuar, empezar, volver al terminar (la voz dice «botón verde»).
  static const verdeContinuar = Color(0xFF2E7D32);

  /// Acento de error / opción incorrecta en actividades.
  static const rojoAcento = Color(0xFFD34836);
}
